interface:
  display_name: "OpenClaw Message Stream"
  short_description: "Configure and run the OpenClaw message stream analyzer plugin."
  default_prompt: "Use the OpenClaw Message Stream skill to generate an OpenClaw config and validate plugin registration."
